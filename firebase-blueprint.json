{
  "entities": {
    "BrochureRequest": {
      "title": "BrochureRequest",
      "description": "Information captured from clients requesting the company executive brochure for lead generation.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Full name of the contact"
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "Work/Corporate email address"
        },
        "company": {
          "type": "string",
          "description": "Name of the electrical wholesale company"
        },
        "roleInterest": {
          "type": "string",
          "description": "Primary service or recruitment role group of interest"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp indicating when the request was made"
        }
      },
      "required": ["name", "email", "company", "roleInterest", "createdAt"]
    }
  },
  "firestore": {
    "/brochure_requests/{requestId}": {
      "schema": "BrochureRequest",
      "description": "Secure CRM leads generated by the Services page brochure request form."
    }
  }
}
